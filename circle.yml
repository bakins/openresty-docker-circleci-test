machine:
  services:
    - docker

dependencies:
  override:
    - true

test:
  override:
    - true
  post:
    - VERSION=${CIRCLE_SHA1} ./build.sh
    - mv openresty:${CIRCLE_SHA1}.tar.gz ${CIRCLE_ARTIFACTS}

